# Database settings.
database:
    # the name of the JDBC driver, mysql in our case
    driverClass:
    # the username
    user:
    # the password
    password:
    # the JDBC URL; the database is called DWGettingStarted
    url:

# Note - details have been left out for security purposes